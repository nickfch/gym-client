import { StyleSheet } from 'react-native';
import { commonWorkoutStyles } from 'src/styles/commonWorkoutStyles'; // Ensure correct import path

export const styles = StyleSheet.create({
  ...commonWorkoutStyles, // Spread common workout styles
});
